{
	"name": "setup-translate5",
	"version": "0.0.1",
	"description": "npx script to setup a translate5 local project",
	"bin": {
		"setup-translate5": "bin.js"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Koundinya Dhulipalla",
	"license": "ISC",
	"dependencies": {
		"chalk": "^4.0.0"
	}
}
